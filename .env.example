# Server
PORT=3000

# PostgreSQL (matches docker-compose.yml service)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=examhub
DB_USER=examhub_user
DB_PASSWORD=changeme

# JWT
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=2h

# bcrypt
BCRYPT_SALT_ROUNDS=10
